import React from 'react';
import { useParams } from 'react-router-dom';

const Edituser = () => {
  
  const { id } = useParams();
  return (´

    <div>
      
    </div>
  );
};

export default Edituser;